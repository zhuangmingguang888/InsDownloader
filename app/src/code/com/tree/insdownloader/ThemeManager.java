package com.tree.insdownloader;

import com.tree.insdownloader.dialog.SelectDialog;

public class ThemeManager {
        private void setTheme(int themeType) {
            switch (themeType) {
                case SelectDialog.DARK:
                    break;
            }
        }
}
